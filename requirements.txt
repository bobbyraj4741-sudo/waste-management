# Core data handling
pandas>=2.0
numpy>=1.24
pyxlsb>=1.0.10        # read the .xlsb Waste Data Interrogator files
openpyxl>=3.1

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Network science
networkx>=3.0
python-louvain>=0.16  # community detection (imported as `community`)

# Machine learning
scikit-learn>=1.3

# Optional / "if time permits" graph-AI tier (roadmap Module 8).
# These do NOT build on Python 3.14 as of this writing; the pipeline
# implements a dependency-light DeepWalk (random walk -> PPMI -> SVD)
# fallback so the graph-embedding path still runs without them.
# node2vec>=0.4
# gensim>=4.3
# torch>=2.0
# torch-geometric>=2.4
